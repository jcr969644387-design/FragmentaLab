name: fragmenta_lab
description: >-
  Fragmenta Lab: Simulador Educativo de Perforacion y Voladura. Aplicacion
  movil educativa y conceptual para estudiantes de Ingenieria de Minas.
publish_to: "none"
version: 1.1.0+2

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/config/education_config.json
